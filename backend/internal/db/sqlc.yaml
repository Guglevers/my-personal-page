version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "generated"
        overrides:
          - column: "posts.created_at"
            go_type: "time.Time"